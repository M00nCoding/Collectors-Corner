<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollectorsCorner"
        tools:targetApi="31">


        <!-- Achievements Layout -->
        <activity
            android:name=".AchievementsActivity"
            android:exported="false" />

        <!-- AddGenre Layout -->
        <activity
            android:name=".AddGenreActivity"
            android:exported="false" />

        <!-- AddBook Layout -->
        <activity
            android:name=".AddBookActivity"
            android:exported="false" />


        <!-- BookInfo Layout -->
        <activity
            android:name=".BookInfoAvtivity"
            android:exported="false" />

        <!-- LoginActivity Login Layout -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- SplashScreenActivity Splash Screen Layout -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity Register Layout -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- HomeActivity Home Screen Layout -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
<!-- End of entire manifest file -->